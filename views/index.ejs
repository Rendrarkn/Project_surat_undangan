<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Daftar Undangan</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-blue-100 min-h-screen">
    <div class="container mx-auto p-8">
        <div class="flex justify-between items-center mb-8">
            <h1 class="text-5xl font-bold text-blue-900">Daftar Surat Undangan</h1>
            <div>
                <span class="text-gray-700">Welcome, <%= user.username %></span>
                <% if (user.role === 'admin') { %>
                    <a href="/add" class="bg-green-500 hover:bg-green-600 text-white font-bold py-3 px-6 rounded-lg shadow-lg transform hover:scale-105 transition duration-300 ease-in-out ml-4">
                        Tambah Undangan
                    </a>
                <% } %>
                <a href="/auth/logout" class="bg-red-500 hover:bg-red-600 text-white font-bold py-3 px-6 rounded-lg shadow-lg transform hover:scale-105 transition duration-300 ease-in-out ml-4">
                    Logout
                </a>
                <% if (user && user.role === 'admin') { %>
                    <a href="/add" class="bg-green-500 hover:bg-green-600 text-white font-bold py-3 px-6 rounded-lg shadow-lg transform hover:scale-105 transition duration-300 ease-in-out ml-4">
                        Tambah Undangan
                    </a>
                <% } %>
            </div>
        </div>
        <div class="bg-white shadow-2xl rounded-lg overflow-hidden">
            <table class="min-w-full divide-y divide-gray-200">
                <thead class="bg-gray-50">
                    <tr>
                        <th scope="col" class="py-3 px-6 text-left text-xs font-semibold text-gray-900 uppercase tracking-wider">Nama Undangan</th>
                        <th scope="col" class="py-3 px-6 text-left text-xs font-semibold text-gray-900 uppercase tracking-wider">Tanggal Acara</th>
                        <th scope="col" class="py-3 px-6 text-left text-xs font-semibold text-gray-900 uppercase tracking-wider">Lokasi</th>
                        <th scope="col" class="py-3 px-6 text-left text-xs font-semibold text-gray-900 uppercase tracking-wider">Deskripsi</th>
                        <th scope="col" class="py-3 px-6 text-left text-xs font-semibold text-gray-900 uppercase tracking-wider">Kategori</th>
                        <th scope="col" class="py-3 px-6 text-center text-xs font-semibold text-gray-900 uppercase tracking-wider">Aksi</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-gray-200">
                    <% invitations.forEach(invitation => { %>
                        <tr class="hover:bg-blue-50 transition duration-300">
                            <td class="py-4 px-6 whitespace-nowrap text-sm font-medium text-gray-500"><%= invitation.nama_undangan %></td>
                            <td class="py-4 px-6 whitespace-nowrap text-sm text-gray-500"><%= new Date(invitation.tanggal_acara).toLocaleDateString('id-ID', { year: 'numeric', month: 'long', day: 'numeric' }) %></td>
                            <td class="py-4 px-6 whitespace-nowrap text-sm text-gray-500"><%= invitation.lokasi %></td>
                            <td class="py-4 px-6 whitespace-nowrap text-sm text-gray-500"><%= invitation.deskripsi %></td>
                            <td class="py-4 px-6 whitespace-nowrap text-sm text-gray-500"><%= invitation.kategori %></td>
                            <td class="py-4 px-6 whitespace-nowrap text-sm text-center">
                                <% if (user.role === 'admin') { %>
                                    <a href="/edit/<%= invitation.id %>" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-md shadow-md hover:shadow-lg transition duration-300">Edit</a>
                                    <a href="/delete/<%= invitation.id %>" class="bg-gray-500 hover:bg-gray-700 text-white font-bold py-2 px-4 rounded-md shadow-md hover:shadow-lg transition duration-300" onclick="return confirm('Are you sure you want to delete this invitation?')">Delete</a>
                                <% } %>
                                <a href="/export-pdf/<%= invitation.id %>" class="bg-red-500 hover:bg-red-700 text-white font-bold py-2 px-4 rounded-md shadow-md hover:shadow-lg transition duration-300">Export to PDF</a>
                            </td>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>